{
  "name": "better-together-website",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs && node serve.mjs",
    "start": "netlify serve"
  },
  "devDependencies": {
    "@fontsource/nunito-sans": "^5.2.7",
    "@tailwindcss/cli": "^4.3.3",
    "prettier": "^3.4.2",
    "tailwindcss": "^4.3.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
